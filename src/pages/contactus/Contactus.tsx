import React from 'react';
import classes from './Contactus.module.scss';

const Contactus = () => {
  return (
    <div className={`${classes.Contactus} border-content-block`}>
    </div>
  );
};

export default Contactus;
